# Artivo · آرتیوو

پلتفرم خلاقیت — مارکت‌پلیس اتصال کارفرماها به طراحان گرافیک و عکاسان.
**Nuxt 4 · Vue 3 · TypeScript · Mobile-first · Persian RTL · Light only**

## اجرا

```bash
npm install         # نصب وابستگی‌ها
npm run dev         # توسعه روی http://localhost:3000
npm run build       # بیلد پرواکشن
npm run typecheck   # چک کامل TypeScript
```

> نکته: در این محیط `npm` به `--legacy-peer-deps` نیاز دارد؛ `yarn install` هم کار می‌کند.

## معماری

```
shared/                    ← کد مشترک اپ و سرور (در فازهای بعد: API)
  types/                   ← تایپ‌های دامنه (تک منبع حقیقت تایپ‌ها)
  config/                  ← کانفیگ‌های محصول
    project-types.ts       ←   انواع پروژه + پریست سایز/فرمت هر نوع
    palettes.ts            ←   پالت‌های رنگی «جهت بصری»
    font-pairings.ts       ←   زوج‌فونت‌های فارسی + لاتین
    pricing.ts             ←   ⚙️ کانفیگ مرکزی قیمت‌گذاری (آماده‌ی پنل ادمین)
    service-categories.ts  ←   تاکسونومی دسته‌ی سرویس‌ها + نگاشت به ویزارد
    job-categories.ts      ←   دسته‌های پروژه‌ی باز + پریست‌های بودجه/مهلت
  services/
    pricing.ts             ← موتور برآورد قیمت — تنها نقطه‌ی محاسبه‌ی قیمت
  data/
    content.ts             ← خلاق‌ها (۱۰ طراح/عکاس)، لوکیشن‌ها
    jobs.ts                ← پروژه‌های باز با بریف کامل (۱۶ بریف)
    portfolio.ts           ← نمونه‌کارها (میزبانی مستقیم روی آرتیوو)
    services.ts            ← سرویس‌های قابل‌سفارش خلاق‌ها
    reviews.ts             ← نظرات کارفرماها
  utils/format.ts          ← فرمت فارسی (عدد، تومان، تاریخ، اعتبارسنجی موبایل)

app/
  assets/css/main.css      ← توکن‌های سیستم طراحی + ریست + کلاس‌های پایه
  components/
    ui/                    ← کیت طراحی: AButton, AInput, ASelect, ASegmented,
                             AChip, ATag, ACheck, ACard, AHeader, ABottomNav,
                             AStepProgress, AEmptyState, ASkeleton, AToaster,
                             AModal, ADrawer, AFilterChips, ARating, AIcon, ...
    home/  cards/          ← بخش‌های صفحه‌ها + کارت‌ها (خلاق، سرویس، نمونه‌کار)
    creatives/             ← گالری نمونه‌کار + لایت‌باکس + نظرات
    create/                ← گام‌های ویزارد درخواست پروژه
  composables/             ← useProjectRequest (وضعیت ویزارد)، usePricing،
                             useRequestSummary، useMyRequests، useToast،
                             useSavedJobs، useJobProposals، useJobsQuery
  pages/
    /                      ← خانه (ادیتوریال)
    /create                ← ویزارد ۸ گامی درخواست پروژه
    /jobs                  ← مارکت‌پلیس پروژه‌های باز (جست‌وجو، فیلترها،
                             مرتب‌سازی، بارگذاری تدریجی)
    /jobs/[id]             ← بریف کامل + ارسال پیشنهاد (قیمت، مهلت، متن،
                             ارجاع به نمونه‌کار/سرویس) با ذخیره‌ی محلی
    /spots · /profile      ← لوکیشن‌ها · درخواست‌ها و پیشنهادهای من
    /creatives             ← فهرست خلاق‌ها (جست‌وجو + فیلتر نوع/دسته/شهر)
    /creatives/[id]        ← پروفایل ادیتوریال خلاق
    /creatives/[id]/portfolio ← گالری کامل نمونه‌کار + لایت‌باکس
    /services              ← مارکت‌پلیس سرویس‌ها
    /services/[id]         ← جزئیات سرویس
  layouts/default.vue      ← هدر/فوتر/ناوبری — مسیرهای /create بدون کروم
  plugins/reveal.ts        ← دایرکتیو v-reveal (ظاهر شدن با اسکرول)
```

## اصول

- **موبایل اول**: همه‌چیز برای ۳۶۰px طراحی و بعد برای دسکتاپ گسترش می‌یابد؛ ناوبری اصلی موبایل، نوار پایین است.
- **زبان بصری ادیتوریال**: پایه‌ی عاجی گرم (`#FAF6EF`)، جوهر تیره، تأکیدهای کورال/ایندیگو/سبز، سرخط درشت، خطوط ظریف — بدون کارت‌های داشبوردی.
- **قیمت هرگز هاردکد نیست**: کامپوننت‌ها فقط `usePricing()` را می‌خوانند. قواعد در `shared/config/pricing.ts` است و `shared/services/pricing.ts` محاسبه می‌کند؛ شکل دیتا برای انتقال به پنل ادمین/دیتابیس آماده است (قیمت پایه، ضریب اندازه، پیچیدگی، فوریت، سرویس‌های اختیاری، حداقل مبلغ).
- **ویزارد تک‌گامی**: هر گام تمامِ صفحه است؛ پیش‌نویس در localStorage؛ خلاصه‌ی زنده در ریل دسکتاپ و کشوی موبایل؛ ویرایش هر بخش از صفحه‌ی بازبینی.
- **دسترس‌پذیری**: RTL بومی با خواص منطقی (`inset-inline`, `margin-inline`)، فوکوس‌رینگ، `aria`، احترام به `prefers-reduced-motion`.
- فقط حالت روشن. بدون پرداخت، چت یا احراز هویت (فاز ۱).

## افزودن آیتم جدید

| می‌خواهی... | کجا |
|---|---|
| نوع پروژه‌ی جدید | `shared/config/project-types.ts` → `projectTypes` + `sizeConfigs` |
| پالت رنگی جدید | `shared/config/palettes.ts` |
| زوج‌فونت جدید | `shared/config/font-pairings.ts` (+ لینک فونت در `nuxt.config.ts`) |
| قانون قیمت جدید | `shared/config/pricing.ts` → در `services/pricing.ts` مصرف کن |
| سرویس اختیاری جدید | `shared/config/pricing.ts` → `addOns` |

## فاز ۱ — وضعیت

✅ سیستم طراحی کامل · صفحه‌های عمومی · ویزارد ۸ گامی درخواست پروژه · برآورد قیمت مرکزی · ثبت شبیه‌سازی‌شده با کد پیگیری (localStorage)

## فاز ۲ — وضعیت

✅ پروفایل ادیتوریال خلاق (طراح/عکاس) با تصویر، بیو، تجربه، زبان‌ها، آمار و CTA
✅ نمونه‌کار چندتصویری (عنوان، توضیح، دسته، تگ، کاور) — میزبانی مستقیم روی آرتیوو، بدون لینک خارجی
✅ گالری کامل + لایت‌باکس (کیبورد/سوایپ RTL) · حالت تصویربزرگ برای پورتفولیو عکاس‌ها
✅ مارکت‌پلیس سرویس‌ها (قیمت شروع، مهلت، اصلاحیه، شامل‌ها، مثال‌ها) + صفحه‌ی سرویس
✅ فیلتر نوع خلاق/دسته/شهر + جست‌وجو · اتصال پروفایل/سرویس به ویزارد (creative=)
✅ ۱۰ آواتار + ۱۰ کاور اختصاصی تولیدشده · نظرات کارفرماها
⏭ بدون چت و پرداخت (طبق دستور) · آپلود واقعی نمونه‌کار با بک‌اند در فاز بعد

## فاز ۳ — وضعیت

✅ مارکت‌پلیس پروژه‌های باز: کشف با جست‌وجو، فیلترهای دسته/نوع خلاق/بازه‌ی بودجه/
   مهلت تحویل/شهر/حضوری-دورکاری + فوری/ذخیره‌شده، ۴ حالت مرتب‌سازی
✅ بارگذاری تدریجی با لایه‌ی کوئری جدا از UI (fetchJobsPage آماده‌ی اتصال به API)
✅ کارت ادیتوریال پروژه: دسته، بودجه، مهلت، محل/دورکاری، توضیح، خلاصه‌ی کارفرما، زمان انتشار
✅ صفحه‌ی پروژه: بریف کامل، الزامات، تحویل‌دادنی‌ها، تصاویر و لینک‌های مرجع،
   کارت کارفرما + پروژه‌های مشابه + نوار اقدام چسبان موبایل
✅ ذخیره/بوکمارک پروژه و ارسال پیشنهاد (قیمت، مهلت، متن، ارجاع‌ها) با
   اعتبارسنجی، ویرایش پیشنهاد و «پیشنهادهای من» در پروفایل
⏭ چت و پرداخت طبق دستور پیاده نشده‌اند.

## فاز ۴ — وضعیت (لوکیشن‌های عکاسی + نقشه‌ی نشان)

✅ کشف لوکیشن عکاسی: نقشه ↔ فهرست (سوئیچر صریح موبایل، نمای دو‌تکه‌ی همزمان دسکتاپ)،
   جست‌وجو، ۱۰ فیلتر نوع عکاسی (پرتره، منظره، خیابانی، معماری، طبیعت، غروب، شب،
   محصول، مد، عروسی)، مرتب‌سازی محبوب/نزدیک‌ترین/تازه‌ها + فیلتر علاقه‌مندی‌ها
✅ «نزدیک‌ترین» با موقعیت کاربر (Geolocation اختیاری) و فاصله‌ی هاورساین به فارسی
✅ صفحه‌ی لوکیشن: گالری بزرگ + لایت‌باکس، نقشه، نشانی، توضیحات، نکته‌ی عکاس‌ها،
   بهترین زمان نور، تگ‌ها، عکس‌های افزوده‌ی کاربران + امتیازدهی ستاره‌ای
✅ افزودن لوکیشن جدید: انتخاب نقطه روی نقشه (حالت pick) + ورودی دستی مختصات،
   دسته‌بندی‌ها، پریست‌های «بهترین زمان»، تگ‌ها، آپلود عکس، دریافت نشانی از نقشه
✅ لایه‌ی نقشه‌ی ماژولار (`useMapProvider`): UI فقط با `MapController` کار می‌کند؛
   آداپتور نشان قابل تعویض با هر پروایدر دیگر بدون تغییر صفحه‌ها
✅ نقشه‌ی نشان: SDK با کلید عمومی از env لود می‌شود؛ پروکسی reverse-geocode سمت
   سرور (`/api/neshan/reverse`) تا کلید REST هرگز به کلاینت نرسد
✅ بدون کلید هم کار می‌کند: fallback کاربردی با مختصات و لینک maps.neshan.org
✅ ۱۰ لوکیشن واقعی ایران با مختصات + هم‌پوشانی localStorage برای عکس/امتیاز/
   علاقه‌مندی/لوکیشن افزوده‌ی کاربر (آماده‌ی اتصال به بک‌اند در فاز بعد)
⏭ چت و پرداخت طبق دستور پیاده نشده‌اند.

⏭ فازهای بعد: بک‌اند و احراز هویت، آپلود واقعی، پنل ادمین قیمت‌گذاری، چت، پرداخت، پنل خلاق‌ها.
